import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));   // Making index.html, The Root Element.
root.render(

    <App />                                                                                                  // Main App which contains all components into it.
);

